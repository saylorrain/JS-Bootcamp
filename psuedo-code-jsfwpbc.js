// Training Library


// Display the available training replays
// Show img, title, desc, register link
// give each training a category, date
// Set up search keywords for each training
// include pagination of trainings 9 per view

// Filter through the trainings based off date, alphabetical, category

// Show quick view when clicked on a training
// Allow modal to close when clicked anywhere off of 
// Create overlay of bg when modal is open
// Allow "page through" of trainings through the modal
